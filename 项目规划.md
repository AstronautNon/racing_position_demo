# 赛车漂移姿态识别 Demo — 项目规划

固定俯视视角下，识别车辆位置、车身朝向与运动方向，并推算**滑移角**。

- 工作目录：`/Users/astronaut_non/Desktop/work/漂移评分系统/demo/demo02`
- 代码与运行由我负责；人工标注、素材采集、盲检由你负责。
- 相关环境：`/opt/anaconda3/envs/racing-pose/bin/python`（已装 geo-trax 1.4.3 / torch 2.14 / ultralytics / opencv）

---

## 1. 目标与成功标准

**一句话目标**：输入一段固定俯视（尽量正射）的漂移视频，输出逐帧的滑移角时序曲线与叠加可视化视频。

**本阶段成功标准**（按重要性排序）：

| 编号 | 标准 | 判定方式 |
|---|---|---|
| S1 | 稳定圆周漂移的滑移角曲线在一段时间内保持基本恒定 | 例如 video03 绕锥桶画圆，稳态段的 β 波动小于 ±5° |
| S2 | 滑移角时序无明显跳变、无 180° 翻转 | 跳变计数、目视检查叠加视频 |
| S3 | 与人工盲标对比，角度误差在可接受范围 | 随机抽 30 帧人工标轴，报 MAE |
| S4 | 对参数（时间窗、阈值）不过度敏感 | 参数扰动测试 |
| S5 | 全流程可复现：一条命令跑完一段视频 | 脚本端到端运行 |

**明确不在本阶段**：漂移评分算法、多车交互、实时部署、跨场地泛化保证。这些留到后续（见 §8）。

---

## 2. 先把概念钉死（我实测 + 查证后的结论）

这一节很重要，前期的弯路多半出在这里。

### 2.1 滑移角是什么

滑移角 β = **车身纵轴朝向** ψ_body 与 **车辆实际运动方向** ψ_vel 之间的夹角：

```
β = wrap180( ψ_body − ψ_vel )
```

俯视图里这两个角来自完全不同的来源：

- **ψ_vel（运动方向）**：由轨迹帧间差分得到，稳定、便宜、已是成熟做法。
- **ψ_body（车身朝向）**：必须从车辆"看起来"的朝向来估计，**这才是本项目的难点**。

整条技术流水线本质上就是"把这两条腿分别做稳，再相减"。

### 2.2 一个能省掉大量标注的结论：只需要"车身轴"

车身轴是**无向的（mod 180°）**。而：

- 漂移中滑移角恒满足 **|β| < 90°**（典型 20°~60°，超过 90° 意味着在倒着走）。
- 所以拿到无向轴之后，只要取"与 ψ_vel 夹角 ≤ 90°"的那一端作为车头方向，就能**自动消解 180° 歧义**，并顺带得到车头/车尾。

**推论**：标注只需标一条**车身轴线**（2 次点击），不需要区分车头车尾；训练也不需要 front/rear 语义标签。这比前期"标车头点 + 车尾点并赋予前后语义"的方案省一半以上工作量，也避开了模型学不会前后语义的风险。

当某一帧 |β| 逼近 90° 时，标记为"可疑帧"，不强行定符号。

### 2.3 坑：geo-trax 的 mode 3/4 旋转框不是车身朝向

geo-trax 的 viz mode 3/4 会画**旋转框**，但官方文档明确说明：该旋转框对齐的是车辆的 **travel direction（行驶方向）**，由"去相机运动后的稳定轨迹"推出。

也就是说，**它给出的是 ψ_vel，不是 ψ_body**。它对我们有用（可作为运动方向的交叉校验），但**不能拿来当车身朝向**，否则算出来的滑移角会恒等于 0。这一点必须先说清楚，避免白跑一轮。

### 2.4 角度测量的前提：需要正射 + 各向同性

图像中量出的角度等于地面真实角度，只有满足"正射视角 + 图像两轴等比例缩放"时才成立。

- 视角一旦倾斜，地面上的圆在图像里变成椭圆，两个方向的缩放不同（各向异性），**角度会产生系统性偏差**，滑移角直接失真。
- 现有的 video02/03 都能看到车身侧面的高度信息，说明有约 10°~25° 的倾角，**需要校正**。

好消息是标定可以无标注完成，而且很稳：

- 地面上的**已知圆**（video03 的靶心同心圆、地面胎痕的圆形轨迹）在图像中呈椭圆，拟合该椭圆即可反推地平面倾角，做圆→圆的重投影校正。
- 更妙的是：圆心轴线的旋转自由度对应的是一个**全局旋转**，而 β 是两个角之差，全局旋转会**自动抵消**。所以标定只需要修掉各向异性，不需要定绝对方向，鲁棒性很高。
- 保底方案：若某段视频找不到可用圆，退化为"假设相似变换"，并在输出里显式标注为"图像角度（未标定）"，绝不冒充地面真值。

### 2.5 坐标与角度约定（沿用 demo01）

- 角度以**图像右方向为 0°**，**顺时针为正**（因为图像 y 轴向下），范围 `[0, 360)`。
- 滑移角 β 同样顺时针为正。
- 所有运动学量在 **stabilized（去相机运动）坐标**上计算；叠加可视化投影回原图。

---

## 3. 现有资产盘点

### 3.1 已可用的

| 资产 | 位置 | 状态 |
|---|---|---|
| geo-trax 1.4.3 源码（可编辑安装） | `demo01/geo-trax-main` | 已装入 racing-pose 环境 |
| 检测模型 `hbb_yolov8s_1920_v1.pt` | HF 缓存 | 已下载，实测可用 |
| video01/02 的轨迹结果 | `demo02/drift/results/*.txt` | 14 列格式 |
| video01/02 的稳定变换 | `*.mov` 同目录 `_vid_transf.txt` | 已可用 |
| 叠加视频 mode_0 | `demo02/drift/results/*_mode_0.mp4` | 已生成 |
| 人工朝向点击工具 | `demo01/preview_heading.py` | 可改造复用 |
| 裁图脚本 | `demo01/extract_vehicle_crops.py` | 可复用 |
| 关键点诊断脚本 | `demo01/keypoint_diagnostic/` | 仅作参考 |

geo-trax 轨迹 txt 列定义（14 列）：

```
frame_id, vehicle_id, x_c(unstab), y_c(unstab), w, h,
x_c(stab), y_c(stab), w(stab), h(stab), class_id, confidence, vehicle_length, vehicle_width
```

### 3.2 前期结论必须继承

前期已实测过两件事，结论要继承，不要重复踩：

1. **geo-trax 检测 + 跟踪可用**，这是可靠的地基。
2. **"检测框 + 关键点联合预测"的 YOLO 路线效果不理想**（5 张图远不够，且联合任务更难）。
   → 前期文档给出的方向是：**保留 geo-trax 做检测与跟踪，把朝向拆成独立模块**。本项目采纳这个方向。
3. 现有标注只有 **5~6 张图**，任何训练都无从谈起，必须先解决数据量。

---

## 4. 素材体检（我实测的结果）

各视频规格与相机运动（实测）：

| 视频 | 分辨率 / 帧率 / 时长 | 视角 | 内容 | 适配度 |
|---|---|---|---|---|
| video01 | 2704×1756 / 59.8fps / 4.5s | 近正射 | 宝马广告，绕标志做持续横滑画圆 | **高**（车大干净；有字幕与水印） |
| video02 | 2704×1756 / 56.8fps / 6.9s | 近正射（略倾） | 场地甩尾，画面叠加了网格十字 | **高** |
| video03 | 2704×1756 / 59.4fps / 8.8s | 近正射（略倾） | KING TEAM E36 绕蓝色锥桶画圆，地面有靶心同心圆 | **最高**（大滑移角 + 稳态圆周 + 现成标定靶） |
| video04 | 912×1616 / 59.9fps / 15.3s | 倾斜俯视 | 林间弯道行驶，车小、有树影 | 低 |
| video05 | 2704×1756 / 60.0fps / 19.1s | 明显斜视 | 漂移赛航拍，车小、浓烟遮挡 | 低（可作难例） |
| video06 | 912×1616 / 60.0fps / 22.0s | 大角度斜视 | 漂移赛竖屏，带社交软件 UI | 低（可作难例） |

**相机运动实测**：video01 相对首帧最大图像平移约 **17 px / 2704 px（≈0.6%）**，video02 约 6 px。→ 可视为**固定机位**，这对我们是好消息。

**两个必须处理的数据问题**：

1. **疑似屏幕录制**：video01/02/04/06 带 UI 元素、水印或黑边，且帧率是 56.8/59.9 这类非常规值。若源内容本身是 30fps 被 60fps 录制，会存在**成对重复帧**。
2. **重复帧的后果很严重**：相邻帧完全相同会让帧间差分速度归零，产生速度与滑移角曲线的假性毛刺。必须检测并按唯一帧重建时间轴。

**结论**：主攻 **video02 + video03**（用户选定），video01 作为第三来源，video04/05/06 留作难例与鲁棒性测试。

> 修订（2026-09-22 第二轮）：本节是初步判断。后续实测推翻了其中两点假设——素材重复帧比例、以及 geo-trax 在 video03 上的可用性。**请以 §12 为准。**

---

## 5. 技术路线（分层）

```
              ┌─ L0 预处理 ─┐
视频 ─────────┤  去黑边/UI │
              │  重复帧检测 │
              │  地面标定   │
              └─────┬──────┘
                    ↓
        ┌─ L1/L2 检测 + 跟踪 + 稳定（geo-trax，已有）─┐
                    ↓
        ┌───────────┴────────────┐
        ↓                        ↓
 L3 运动方向 ψ_vel          L4 车身朝向 ψ_body ★难点
 （稳定轨迹差分 + 平滑）    （裁剪图 → 朝向估计）
        └───────────┬────────────┘
                    ↓
        L5 滑移角 β = wrap180(ψ_body − ψ_vel)
               + 平滑 + 质量标志
                    ↓
        L6 输出：逐帧 CSV / 叠加视频 / 曲线 / 精度报告
```

### L0 预处理

- 自动检测并裁掉黑边与 UI 区域（按行列方差定位有效画面）。
- **重复帧检测**：计算相邻帧的像素差，差异低于阈值判定为重复帧，输出报告。
- **时间轴重建**：以唯一帧序列重建时间戳，后续所有速度都基于重建后的时间轴。
- **地面标定（可选但推荐）**：从已知圆（video03 靶心）拟合椭圆 → 求各向异性校正单应 → 输出 `calib.json`；失败则退化为相似变换并在输出中显式标记。

### L1 / L2 检测、跟踪、稳定

> 修订（见 §12.2/12.3）：原本假设"geo-trax 检测可直接复用"，实测只在 video01/02 成立，video03 完全失效。因此 L1 改为**双分支**。

- **分支 A：geo-trax**（保留）。适用于 geo-trax 模型能检到的场景（当前是 video01/02），优点是有现成的跟踪与 ID、有稳定坐标输出。待办：video04~06 择机补跑归档。
- **分支 B：静止机位背景建模 + 跟踪**（新增，零训练，已实测）。中值背景建模 → 帧差 → 形态学 → 连通域 → 最大连通域为车 → 跨帧关联。适用于**相机静止**的素材（实测 01/02/03 全部成立），是 video03 的**唯一可行路线**。副产品：车辆掩膜。
- **稳定化按需启用**：相机静止时（01/02/03）不需要，且 geo-trax 在 video03 上因重复砖纹产出退化单应（§12.2），应当关闭。
- 关键输出：统一为**去相机运动后的图像坐标轨迹** `(frame, t, x, y, w, h, conf, mask_ok)`，两个分支产出同一套接口，下游无感。
- 交叉校验：两条分支在 video01/02 上应给出相近的轨迹与 ψ_vel；明显不一致说明其中一条有问题。

### L3 运动方向 ψ_vel【本次要写】

- 输入：稳定轨迹 `(frame, x_c(stab), y_c(stab), t)`。
- 方法：**局部加权多项式拟合（Savitzky–Golay 一阶导）**，或 Kalman 滤波（常速/常加速模型）。先用前者，可解释性好、调参直观。
- 关键调参点：
  - **时间窗长度**：漂移中航向变化快，窗口过长会产生相位滞后。需用"稳态圆周段 β 应恒定"来反向定窗长。
  - **加权**：以检测置信度加权，抑制低置信度帧的抖动。
- 输出：每帧速度矢量 `(vx, vy)`、速率 `|v|`、航向 `ψ_vel`。
- 校验：静止车辆速率应≈0；video03 的圆周运动曲率半径应基本恒定且与靶心半径量级一致。

### L4 车身朝向 ψ_body【本次要写，全项目难点】

四种候选路线，按**标注成本从低到高**排列。策略是先从零标注方案拿基线，再按需升级。

**4A. 现成 OBB 模型直接推理（零标注）**
用 ultralytics 的 `yolo11n-obb.pt`（DOTA 预训练，含车辆类）直接在裁剪图上推理，得到旋转框角度。
- 优点：立刻能跑，零标注，先量出一版基线数字。
- 风险：DOTA 是高空小目标，与航拍大车域差较大，可能很差。**但"很差"也是有价值的信息**。

**4B. 裁剪图上的轴角回归/分类小网络（零标注起步 → 少量标注修正）**
- 输入：以车辆中心裁一块**正方形**区域（约 1.3×max(w,h)）。
  - 为什么必须正方形：轴对齐框（HBB）的长宽比本身强烈泄露了车身轴角，如果直接用 HBB 裁剪，网络会学到"看裁剪框形状"这个捷径，在检测框抖动时立刻崩溃。正方形裁剪强制网络去看**车本身**。
- 输出：0°~180° 的轴角。推荐做**分桶分类**（如 90 桶 × 2°），用 softmax 期望求连续角，比直接回归稳且能给置信度。
- 训练数据两种来源，可叠加：
  - **自监督伪标签（零人工）**：从轨迹中筛"近似直行"的帧（应满足 β≈0），用 ψ_vel 当伪标签。注意 video03 几乎全程在大滑移角，直行帧稀少，伪标签主要得靠 video02/04 等素材，可能需要补充直行素材。
  - **少量人工标注（150~300 张）**：你点一条车身轴线（2 次点击），见 L6 标注工具。
- 训练细节：按**来源视频分组划分** train/val/test，绝不从同一视频的邻近帧制造独立测试集（这条前期文档已强调，继续遵守）。

**4C. OBB 检测器微调（标注成本中等，工程性最好）**
用少量 OBB 四点标注（CVAT 支持）微调 `yolo11n-obb`，端到端输出旋转框。
- 优点：绕开"HBB 裁剪 + 角度回归"的两段误差，且旋转框本身对遮挡更鲁棒。
- 代价：四点标注比两点轴线贵，但仍是可控量级（200~400 张量级）。

**4D. 经典方法（仅作诊断，不做主线）**
车辆掩膜 PCA、模板匹配、车顶/车窗布局分析等。遇浓烟、遮挡、阴影立刻崩，只在调试时用来对照。

**推荐执行顺序**：`4A 量基线` → `4B(伪标签) 拿第一版` → `人工标 150~300 张 → 训练 + 独立测试` → `不够再上 4C`。

### L5 滑移角

- `β = wrap180(ψ_body − ψ_vel)`，顺时针为正。
- 取"与 ψ_vel 夹角 ≤90°"的轴端作为车头 → 自动消解 180° 歧义（见 §2.2）。
- 平滑：短窗中值滤波去掉孤立跳变，再做轻度低通。
- **质量标志**（每帧输出，便于后续过滤与解释）：

| 标志 | 含义 |
|---|---|
| `low_conf` | 检测置信度低于阈值 |
| `no_measure` | 该帧无法量测（遮挡、出画等） |
| `edge` | 车辆贴边或出画 |
| `ambiguous_dir` | \|β\| 接近 90°，符号不可信 |
| `heading_uncertain` | 朝向模块自身置信度低 |
| `dup_frame` | 重复帧 |
| `uncalibrated` | 未做地面标定，角度为图像角度 |

### L6 输出与评估

**输出物**

1. 逐帧 CSV：
   `frame, t, track_id, x_stab, y_stab, speed_px_s, speed_m_s?, psi_vel, psi_body, heading_conf, beta_deg, flags`
2. 叠加视频：车辆框 + 车身轴线 + 速度矢量箭头 + β 数值（顺时针为正，红蓝区分正负）。
3. 曲线图：β 时序 + 速率时序（叠加显示，便于看"高速大滑移"段）。

**评估（没有真值时怎么证明对不对）**

1. **物理合理性校验**（最重要、且完全自足）：
   - video03 绕锥桶画圆的稳态段，β 应基本恒定；波动大于 ±5° 就说明噪声过大。
   - 静止/低速段 β 应无意义（速度太小时方向不可定义）→ 检查是否被正确标记。
   - 视频起点与终点 β 不应有 180° 跳变。
2. **人工盲标抽检**：随机抽 30 帧（跨视频、跨朝向），你盲标轴线，算角度 MAE 与粗差比例。这是唯一的"准真值"。
3. **仿真真值（强烈建议，见 §7）**：游戏俯视回放 + 遥测滑移角真值，可给出真正的精度数字。
4. **参数敏感性**：时间窗、平滑强度、置信度阈值各扰动 ±50%，看 β 曲线变化幅度。

---

## 6. 交付物清单

| 编号 | 交付物 | 内容 |
|---|---|---|
| D1 | 预处理模块 + 体检报告 | 黑边/UI 裁剪、重复帧统计、有效时长 |
| D2 | 全视频轨迹数据 | geo-trax 对 6 段视频的轨迹 + 稳定变换，归档到 `data/tracks/` |
| D3 | 地面标定模块 | 圆→圆标定，输出每视频的 `calib.json` 及校正前后对比图 |
| D4 | 运动方向模块 | L3，含校验报告（静止/圆周/曲率） |
| D5 | 车身朝向模块 | L4，含零标注基线结果与最终模型 |
| D6 | **人工标注工具** | 你用的工具：点轴线、翻帧、可续标、按来源分组导出 |
| D7 | 滑移角主程序 | 一条命令产出 CSV + 叠加视频 + 曲线图 |
| D8 | 精度报告 | 物理校验 + 盲标 MAE + 参数敏感性 +（可选）仿真真值对比 |
| D9 | 素材建议与采购清单 | 见 §7 |

---

## 7. 素材方向建议（你去找）

现有 6 段里只有 3 段真正适合做主线，建议按下面方向补充。

### 优先级 1：**定机位正俯拍**

要点：**相机不动**，**镜头垂直向下或接近垂直**，车在画面内完整，车与镜头之间没有浓烟。

- 直接搜：`drone top down drift`、`drone hovering donut drift`、`skid pad overhead drone`、`俯瞰 漂移 无人机`、`定机位 漂移`、`drift donut top view`。
- 场景关键词比设备关键词更有用：`skid pad`、`donut`、`figure 8`、`autocross`、`test track`。
- 你现有的 video03 就是这一类里的优秀样本（地面还有靶心，能直接做标定）。
- 注意避开：**FPV 追逐机**、**跟随航拍**、大角度斜拍、烟雾把车整个糊住的镜头。

### 优先级 2：**自己拍一段（最可控，强烈建议）**

- 设备：手机或相机装在三脚架 / 伸缩杆 / 高处桅杆（5~8 m），架在甩尾场地边上，**垂直向下或尽量垂直**。
- 拍摄动作按顺序做，便于分离各环节问题：
  1. 车**低速直线**通过（滑移角应≈0，用来验证 ψ_vel 和朝向模块在"零滑移"下的正确性）
  2. 车**定圆匀速漂移**（滑移角应基本恒定，用来验证稳态精度）
  3. 车**八字 / 连续换向**（滑移角快速变号，用来验证动态响应与时延）
- 地面上**画一个已知尺寸的圆或方格**（粉笔/胶带即可）：一举解决标定与像素→米的比例问题。
- 不要隔着烟拍；尽量均匀光照，避免车辆长时间落在树影里。

### 优先级 3：**仿真真值（唯一能给出"精度数字"的路子）**

真实素材没有滑移角真值，只能做物理合理性校验。若要在报告里写出"MAE = X°"，最实际的办法是：

- **Assetto Corsa** / **rFactor 2** / **BeamNG.drive** / **CarX Drift Racing**：跑一段漂移，用**俯视自由相机**录屏，同时导出遥测（AC 有 shared memory 与第三方 telemetry 记录器，BeamNG 可直接导出 CSV），里面含真实 slip angle。
- 这样就有了**同一时刻的"图像"和"真值"**，可以做真正的定量评估，也可以低成本大量生成训练数据。
- 注意：仿真画面的车辆外观与真实车差异大，**不适合当训练主数据**，但非常适合**当评估真值**和**验证算法正确性**。

### 优先级 4：**朝向预训练的补充素材**

任何**正射/俯视交通视频**都能用来扩充"车身轴角"数据集——人类判断俯视车辆的朝向非常容易，标注成本低。可用于预训练，再在赛车素材上微调。

### 明确不推荐

- 带 UI 覆盖层、黑边、水印的**屏幕录制**视频（现有 video01/02/04/06 有嫌疑），会引入重复帧与干扰区域。
- 浓烟遮挡、车辆频繁出入画面、镜头跟随运动的素材。

---

## 8. 里程碑

| 阶段 | 内容 | 产出 |
|---|---|---|
| **M0** | 环境与数据体检（**本次已基本完成**） | 本规划文档 + 素材适配度结论 |
| **M1** | 骨架打通：预处理 + 全视频 geo-trax 归档 + L3 运动方向 | D1 / D2 / D4，先出速度与航向曲线（**暂无滑移角**） |
| **M2** | 朝向零标注基线：4A 直接推理 + 4B 伪标签训练 + 180° 消歧 | D5 的基线版本 |
| **M3** | 标注工具 + 人工标 150~300 张 → 训练 → 独立测试集评估 | D5 正式版本 + D6 |
| **M4** | 全流程合成 + 地面标定 | D3 / D7：CSV + 叠加视频 + 曲线 |
| **M5** | 精度报告 + 素材扩充 | D8 / D9 |
| **M6**（可选） | OBB 微调（4C）、评分模块、实时化 | — |

**建议在 M1 结束后先看一版"速度 + 航向"曲线再决定 M2 的投入**——因为 M1 的产物能立刻暴露素材本身的问题（重复帧、跟踪断裂、比例尺），比先做模型更划算。

---

## 9. 分工

**我做（代码与实验）**

- L0 预处理、重复帧检测、时间轴重建、地面标定
- L1/L2 geo-trax 批量运行与结果归档
- L3 运动方向模块 + 校验
- L4 朝向模块（含零标注基线、训练脚本、评估脚本）
- D6 人工标注工具
- D7 主程序、叠加视频、曲线图
- D8 精度报告

**你做（人工）**

- **素材采集**：按 §7 补充视频，优先"定机位正俯拍"
- **人工标注**：用我给的标注工具，每张图**点一条车身轴线（2 次点击）**，目标 150~300 张，先标 video03，再标 video02/01
- **盲标抽检**：随机抽 30 帧独立标注，用于算误差（这些帧不参与训练）
- **决策**：见 §11
- （可选）**拍摄一段自控素材**，按 §7 优先级 2 的动作脚本

---

## 10. 风险与取舍

| 风险 | 影响 | 应对 |
|---|---|---|
| 素材重复帧 / 屏幕录制 | 速度与 β 曲线出现假毛刺 | L0 强制检测并重建时间轴；必要时请你换素材 |
| 视角倾斜导致角度失真 | β 系统性偏差 | 地面圆标定；无圆则显式标记"未标定" |
| 朝向模块在小样本下过拟合 | 数字好看但没意义 | 严格按来源分组划分；独立测试集；只用盲标 MAE 汇报 |
| 浓烟 / 遮挡 | 朝向估计崩溃 | 输出质量标志；不硬猜，标 `no_measure` |
| DOTA 预训练域差过大（4A） | 零标注基线可能很差 | 视为基线而非最终方案；4B/4C 兜底 |
| 缺少真值 | 无法给出可信精度 | 物理合理性校验 + 盲标抽检 + 仿真真值（§7 优先级 3） |
| 低速时方向不可定义 | β 无意义但看起来有数 | 设速度下限，低于阈值输出 `no_measure` |

---

## 11. 待你决策的三件事

1. **主攻视频**：建议 **video03**（正射、有靶心可标定、稳态圆周便于校验）。是否同意？还是要换？
2. **标注预算**：能接受的人工标注量级是多少？（建议 150~300 张起步；越低越依赖伪标签，精度上限越低）
3. **是否引入仿真真值**：愿意跑一次游戏的俯视回放 + 遥测导出吗？这是拿到"精度数字"的前提，不做的话报告里只能写物理合理性与盲标误差。

### 决策结果（2026-09-22）

| 项 | 结论 |
|---|---|
| 主攻视频 | **video02 为主，video02 + video03 一起做主线**（用户选定；video03 的检测问题见 §12.2，需单独解决） |
| 仿真真值 | **不做**。当前定位为基础 demo，后续再迭代 |
| 标注预算 | 待定（按 §12.4，现有素材约可支撑 250 张量级） |

下一步从 **M1** 开始：预处理 + 双分支检测（§5 L1 修订）+ 运动方向模块。

---

## 12. 第二轮实测补充（2026-09-22）

素材真实性核查 + 检测层可行性验证。**本节结论覆盖前文对应描述。**

### 12.1 六段素材全部是屏幕录制，50%~72% 的帧是重复帧

检测方法：相邻帧缩到 240×160 灰度，平均绝对差 < 0.5 判为重复帧。

| 视频 | 总帧 | 去重后唯一帧 | 重复率 |
|---|---|---|---|
| video01 | 272 | 75 | 72.5% |
| video02 | 390 | 135 | 63.4% |
| video03 | 525 | 256 | 50.8% |
| video04 | 916 | 410 | 55.2% |
| video05 | 1146 | 454 | 60.3% |
| video06 | 1319 | 542 | 58.8% |

- video01 还有大面积黑边：内容是竖屏嵌在横屏里，左右各约 879 px / 68 px 黑边，实际有效宽度只有约 1757 px。
- 重复帧把有效动作时长压缩到 **1.3 s ~ 9.0 s**。
- **行动项：后续素材必须取原始视频文件，不要再二次录制。**

### 12.2 geo-trax 在 video03 上完全失效（推翻了原假设）

已对 video03 跑完整流程，结果归档在 `drift/results/video03.*`，但**该结果不可用**：

- 3271 条检测里绝大多数是 19×24 px 的地面纹理误检（红砖格 + 靶心圆环所致）。
- 真正的车只在 frame 349~358 附近被检出 10 帧（ID 65，置信度 0.5~0.7，框 1074×261 明显偏移）。
- **frame 150、frame 400 整车在画面中清晰完整，检测数为 0。**
- 根因：这台车占画面宽度约 **42%**（1022×578 px / 2704×1756），远超模型训练时的航拍小目标尺度。**调 imgsz 无法解决**，因为目标相对尺度与 imgsz 无关。
- 附带问题：geo-trax 的稳定化在 video03 上产出退化单应（行列式 0.147 ~ 8.5e4），日志报 `Invalid transforms`。原因是地面重复砖纹导致特征匹配退化。相机本身静止，所以稳定化对 video03 既无必要也有害。

### 12.3 新增路线：静止机位背景建模（零训练，已实测）

因为 01/02/03 的相机确实是静止的，用中值背景建模 + 帧差 + 形态学 + 连通域，取最大连通域：

| 视频 | 检查帧 | 最大连通域 | 判定 |
|---|---|---|---|
| video01 | 60 / 150 / 240 | 62×94 / 58×117 / 68×55 | **命中** |
| video02 | 120 / 240 | 84×110 / 96×106 | **命中** |
| video03 | 150 / 262 / 400 | 263×216 / 264×141 / 235×57 | **命中** |
| video04 | 200 / 500 / 800 | 占整帧 91%~95% | 失效：相机在动 |
| video05 | 300 / 700 / 1000 | 横贯整幅的大块 | 失效：相机在动 + 人群 |
| video06 | 300 / 800 / 1200 | 占整帧 65%~92% | 失效：相机在动 |

→ **video01/02/03 用背景建模即可零训练定位车辆**；video04/05/06 相机真在动，且本身是斜视难例，暂时搁置。

附带收益与限制：背景建模给出的是**车辆掩膜**，对掩膜做 PCA 可得车身主轴（零标注基线候选）。但实测 video03 的掩膜主轴标准差达 71°，**噪声很大，只能当粗糙初值，必须用人工标注校准**，不可直接当结果使用。

### 12.4 修正后的可用裁图统计（"够不够"的直接答案）

口径：只取去重后的唯一帧；只保留车本身完整在画面内的帧；以车辆中心取正方形裁片；"视觉互不重复"用 64×64 灰度归一化互相关贪婪筛选。

| 视频 | 唯一帧 | 背景/检测可用 | 完整在画面内 | 互不重复 @0.99 / @0.95 / @0.90 |
|---|---|---|---|---|
| video01 | 75 | 75 | 75 | 75 / 73 / 56 |
| video02 | 135 | 131 | 122 | 122 / 122 / 93 |
| video03 | 256 | 256 | 230 | 138 / 55 / 31 |

合计（@0.95）：**约 250 张视觉上互不重复的车身裁图**。

朝向覆盖（用轴对齐框长宽比反推轴角，仅作粗略代理）：video02 的轴角在 0°~90° 上分布较均匀（标准差 27°），单段素材内朝向覆盖尚可。

### 12.5 对整体结论的影响

1. **起步量：够，但很薄。** 约 250 张达到 150~300 的建议下限，但来源只有 3 段（且 video02/03 是同一场地）。按"按来源分组划分"的规矩，最多做出 3 源划分 → train 2 源 / test 1 源。**任何泛化结论都不成立，只能算同源近似验证。**
2. **检测层不再是"已解决的地基"。** geo-trax 只在 video01/02 有效。L1 需要增加背景建模分支，并保留 geo-trax 作为有相机运动场景的后备。
3. **video03 仍值得投入**：车最大（1022×578 px，朝向估计精度潜力最高）、有靶心可标定、是稳态圆周（S1 校验的最佳素材），值得为它单独解决检测。
4. **未验证项**：COCO 通用检测器路线因权重下载停滞 12 分钟被终止，尚未验证。它决定的是"相机运动场景"下的检测能力，不影响静止机位主线的推进，可择机重试。

## 13. 第三轮素材体检：新增 9 段（2026-09-23）

素材库从 6 段扩到 15 段（`video07~15`）。**本节结论覆盖 §12 相应的统计口径与主线建议。**

### 13.1 判据修正（先说要紧的）

§12.1 用的重复帧判据是"相邻帧缩到 240×160 灰度、平均绝对差 < 0.5"。这个判据在**目标很小时会严重高估重复率**：实测 video07（车身仅占画面宽 3.0%）被判为 95.2% 重复、41 帧连续相同，但抽帧显示车在明显移动——小目标缓慢运动对全帧均值的影响低于阈值。

改用**变更像素占比判据**（480×270 灰度下 `|diff| > 20` 的像素占比 < 0.05% 判为重复帧）重测全部 15 段：

| 视频 | 旧判据（均值） | 新判据（变更占比） | 差异解读 |
|---|---|---|---|
| video01 | 72.2% | 71.5% | 一致 |
| video02 | 63.2% | 60.8% | 一致 |
| video03 | 50.7% | 50.3% | 一致 |
| video04 | 53.5% | 13.3% | 旧判据把缓慢运动误判为重复 |
| video05 | 60.3% | 50.9% | 同上，量级偏差 |
| video06 | 58.6% | 29.2% | 同上，量级偏差 |
| video07 | 95.2% | **9.5%** | 旧判据完全失效 |

- **§12.1 对 video01/02/03 的结论无需推翻**（新判据复现了同样的数字，"屏幕录制 + 一半重复帧"成立）。
- 但旧判据在**小目标**和**慢运动**两类画面上不可靠，**即日起统一采用"变更像素占比"判据**，后文所有统计口径以此为准。

### 13.2 新增素材规格与体检

相机位移 = 相位相关法测得的、相对首帧的最大累计平移（占画面宽比例）。

| 文件 | 分辨率 | fps | 总帧 | 唯一帧 | 重复率 | 有效时长 | 相机位移 | 判定 |
|---|---|---|---|---|---|---|---|---|
| video07 | 1920×1080 | 25 | 42 | 38 | 9.5% | 1.5 s | 2.6 px (0.14%) | 静止，但极短 |
| video08 | 1920×1080 | 25 | 92 | 83 | 9.8% | 3.3 s | 2.1 px (0.11%) | 静止 |
| video09 | 1920×1080 | 25 | 33 | 30 | 9.1% | 1.2 s | 4.2 px (0.22%) | 静止，车大，太短 |
| video10 | 1920×1080 | 30 | 479 | 422 | 11.9% | 14.1 s | 280 px (14.6%) | **相机在动** |
| video11 | 1920×1080 | 30 | 316 | 280 | 11.4% | 9.3 s | 246 px (12.8%) | **相机在动** |
| video12 | 1920×1080 | 25 | 421 | 291 | 30.9% | 11.6 s | 23 px → 137 px | 前 75% 静止，结尾镜头切换 |
| video13 | 640×360 | 25 | 162 | 93 | 42.6% | 3.7 s | 2.7 px (0.42%) | 静止，**分辨率过低** |
| video14 | 640×360 | 25 | 106 | 102 | 3.8% | 4.1 s | 5.1 px (0.80%) | 静止，**分辨率过低** |
| video15 | 1920×1080 | 25 | 115 | 111 | 3.5% | 4.4 s | 16.8 px (0.88%) | 静止，车大，但黑边 + 水印 + 片头 |

**新素材的重复帧率是 3.5%~31%（有一段 42.6%），比旧素材的 50%~72% 好一个量级**——说明这次是直接下原文件，采集方式对了。

### 13.3 内容判定（抽帧目视）

| 文件 | 内容与视角 | 可用性 |
|---|---|---|
| video07 / 08 / 09 | 英文驾驶教学，红色车在湿滑**圆形 skid pad** 上做定圆。07/08 是全景点机位，车仅占宽 3.0%~5.6%；09 是同场地近景**正俯视**，车占宽 20.6% | 07/08 车太小，价值低；09 好但仅 1.2 s |
| video10 / 11 | 中文教学，多锥桶 skid pad，画面带 A.R.T 水印与中文字幕，**相机在绕车运动** | 归难例，但是检验"相机运动下方案是否成立"的好素材 |
| video12 | 中文教学，红色车在碎石/泥地做定圆，**正俯视无人机**，车占宽 19.7% | **好，新的主力候选** |
| video13 / 14 | 中文教学，蓝色车在混凝土场地做**八字**，地面留有清晰轮胎痕（∞ 形）。正俯视 | 动作覆盖最好，但**分辨率不够** |
| video15 | 中文，蓝色车定圆，地面有轮胎圆环痕，正俯视，车占宽 25.6%。**是录屏**，带"大神"水印、上下黑边、片头数字倒计时 | 车够大，但需裁黑边、去片头 |

地面轮胎痕这件事有额外价值：**它是天然的场地参考物**，可用于校验轨迹是否闭合、以及做比例尺粗标定。

### 13.4 可用裁图统计（静止机位）

口径同 §12.4：只取唯一帧；只保留车完整在画面内；以车为中心取正方形裁片；"互不重复"用 64×64 灰度归一化互相关贪婪筛选 @0.95。

| 视频 | 唯一帧 | 定位到车 | 互不重复 | 车身像素中位 | 车占画面宽 |
|---|---|---|---|---|---|
| video01 | 77 | 77 | 70 | 272×328 | 10.1% |
| video02 | 144 | 122 | 106 | 406×392 | 15.0% |
| video03 | 258 | 232 | 54 | 1022×580 | 37.8% |
| video07 | 38 | 16 | 14 | 58×106 | 3.0% |
| video08 | 83 | 78 | 53 | 108×58 | 5.6% |
| video09 | 30 | 29 | 28 | 395×212 | 20.6% |
| video12 | 291 | 45 | 42 | 378×256 | 19.7% |
| video13 | 93 | 93 | 24 | 180×25（可疑） | 28.1% |
| video14 | 102 | 102 | 76 | 72×75 | 11.2% |
| video15 | 111 | 43 | 28 | 491×315 | 25.6% |
| **合计** | **1227** | — | **495** | | |

→ 从 §12.4 的约 250 张提升到 **495 张**，达到起步量（150~300）的 1.6~3.3 倍。

**三处必须打折，别把 495 当净数**：

1. **video13 / video03 的掩膜质量差**：video13 掩膜中位 180×25 px，明显把轮胎痕/阴影并进了车辆；video03 掩膜 1022×580。裁片定心不准，需检测器复核后才能用于标注。
2. **video07 / 08 车太小**（占宽 3.0%~5.6%），朝向标注的角误差会被放大，实际训练价值低。
3. **video13 / 14 只有 640×360**，车身仅 50~72 px，标注精度上限低。
4. video12 的 291 唯一帧只有 45 帧定位到车、且互不重复仅 42——**说明该段绝大部分帧里车不在画面内或被子采样漏掉**，有效密度不高（有效 11.6 s 是时长，不是样本量）。
5. 掩膜 PCA 主轴的标准差仍普遍 44°~83°，**继续维持 §12.3 的结论：主轴只能当粗初值，必须靠人工标注校准。**

### 13.5 素材来源去重（决定划分方式，很关键）

按内容与场地判断，新增 9 段实际只对应 **5 个独立来源**：

- **S1** = video07 + 08 + 09（英文 skid pad 教学，同场地同次拍摄）
- **S2** = video10 + 11（中文 A.R.T 教学）
- **S3** = video12
- **S4** = video13 + 14
- **S5** = video15

加上原有的 video01（宝马广告）、video02、video03（02/03 同场地但应为不同源），以及难例 video04/05/06。

**这改善了一件关键的事**：独立来源数从 3 个提升到 **8~9 个**，"按来源分组划分"从此可以凑出真正的 train / val / test 多源划分，不再像 §12.5 那样只能做 3 源勉强切分。

仍需保留的限制：video02 与 video03 同场地；S4 内部两段是同一套动作。**结论可称为"跨来源验证"，但不能声称跨场地泛化。**

### 13.6 修正后的主线建议（覆盖 §4 / §11 的结论）

原主线 video02 + video03，建议调整为 **video12 + video15 + video02 + video03**，其余降为辅助与难例：

| 定位 | 素材 | 理由 |
|---|---|---|
| **主力** | **video12** | 正俯视、有效 11.6 s（最长）、车 378×256 px、构图稳定。**前提：截掉尾部镜头切换** |
| **主力** | **video15** | 正俯视、车 491×315 px（第二大）。**前提：裁黑边（上 31 / 下 52 px）+ 去掉片头倒计时与"大神"水印** |
| **主力** | **video02** | 质量最好，但有效仅 2.5 s，样本贡献有限 |
| **专用** | **video03** | 车最大（1022×580 px），朝向精度潜力最高，且是稳态圆周（S1 校验最佳）。必须为它单独解决检测 |
| **样例** | **video09** | 车大、正俯视，适合当定性演示片段 |
| **待回源** | **video13 / 14** | 动作（八字）价值最高，若拿到 1080p 原画可直接升为主线 |
| **难例** | **video10 / 11** | 相机在动，用于检验方案在相机运动下的鲁棒性 |
| **暂搁** | **video04 / 05 / 06** | 斜视 + 相机运动 + 低空人群，暂不投入 |

### 13.7 下一步

1. **素材补充只剩两件事**：(a) video13/14 的 1080p 原画（现在 640×360 太糊）；(b) video12/15 的无字幕无片头版本。两者都不是阻塞项。
2. 分工与里程碑（§8 / §9）不变，**可以从 M1 开工**：预处理（裁黑边 + 重复帧剔除 + 时间轴重建）、双分支检测（背景建模 + geo-trax 后备）、运动方向模块，先产出速度与航向曲线，无需任何人工标注。

---

## 14. M1 实施记录与 L4 标注链路（2026-09-23）

### 14.1 M1 已完成

预处理（黑边/去重/时间轴重建/背景可用性判定）、双分支检测、运动方向 ψ_vel
全部跑通，8 段主线素材检出覆盖率 93%~100%。与 geo-trax 交叉校验：video02 IoU 中位 0.933
（自动匹配到此前人工确认的 ID 31），video01 0.763；video03 的 0.001 是预期的（§12.2 已说明该段
geo-trax 失效）。详见 `outputs/reports/M1_报告.md` 与 `README.md`。

### 14.2 选帧门槛：一条被实测推翻的判据

第一版用它来剔除"车身被画面截断"的帧：**车框距画面边缘留白 < 1% 即剔除**。
结果 video15 只剩 **7 个候选**（检出 96 帧）、video12 只剩 96 个（检出 265 帧），
主线素材几乎标不动。

量了才知道问题所在：检测框会被**车身阴影**撑大（video15 框宽比车身核心宽 18%，
p90 达 2.07 倍），框"贴边"往往只是阴影尾巴越界，车本身完整在画面内。

改用**越界幅度**（框超出画面的量 / 框长边）来判：

| 素材 | 出界帧的越界幅度 p50 / p90 / max | 旧判据候选 | 新判据（≤10%）候选 |
|---|---|---|---|
| video15 | 7.6% / 13.1% / 15.2% | 7 | **42** |
| video12 | 2.8% / 10.2% / 12.9% | 96 | **186** |
| video03 | 2.4% / 3.2% / 11.8% | 123 | **192** |
| video02 | 6.0% / 6.5% / 6.6% | 100 | 104 |

取 10% 是有依据的取舍：最多 10% 的车身长度在画面外，剩下 90% 的可见部分仍足以定出车身轴；
真正被截去大半的车仍会被剔除。标注台里保留了逐帧提示（`cut` 标记 + 侧栏警示），
标不准的帧按 `N` 跳过即可 —— 自动筛选负责"提出候选"，人负责"否决"。

### 14.3 标注预算与队列（已生成）

配额按"样本量 × 朝向精度潜力"分配，`src/config.py` 的 `ANNOT_QUOTA`：

| 素材 | 队列帧数 | 说明 |
|---|---|---|
| video12 | 55 | 样本最多（186 个候选） |
| video02 | 50 | 质量最好 |
| video03 | 45 | 车最大，朝向精度潜力最高 |
| video15 | 35 | 车第二大；有效候选 42 个 |
| video01 | 25 | 辅助 |
| video09 | 20 | 演示片段 |
| **合计** | **230** | 落在 §11 定的 150~300 起步区间 |

选帧用**贪心最远点采样**（朝向代理 / 画面位置 / 时间 / 外观 四项特征），
效果对比见 `outputs/plots/<名>_annot_queue.png` 顶部的直方图：
默认候选的朝向分布有尖峰，选完之后明显被拉平。

### 14.4 L4 交付的标注链路

| 模块 | 作用 |
|---|---|
| `src/select_frames.py` | 选帧 + 队列 CSV + 预览图 + 预渲染裁图 |
| `src/crops.py` | 裁图渲染与"显示↔工作图"坐标换算（标注台与预览图共用） |
| `src/annotate.py` | 标注台：浏览器点两下，`--serve` 边点边存 / `--export` 自包含离线 |

设计上只解决"人做什么"这一件事：**沿车身点两个点**，车头车尾交给程序推
（§2.2 的 180° 歧义自动消解）。界面同时给出运动方向箭头与 PCA 提示轴，
但两者只是参考 —— 提示轴可一键采用（记为 `src=hint`，与人工点选的 `src=manual` 分开存档）。

### 14.5 仍需你出手的两件事（不变）

1. video13/14 的 1080p 原画（八字动作价值最高，现在 640×360 不足以标注）
2. video12/15 的无字幕、无片头版本（现有版本可用，但水印会进裁图）

其余全部就绪：**接下来只等标注结果**。标完 `outputs/annotations/<名>.csv` 后重跑
`python -m src.pipeline --all-static`，β 曲线就会从"PCA 基线（不可用）"换成人工标注结果。


